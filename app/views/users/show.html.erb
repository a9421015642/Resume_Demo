<%= render 'shared/user_show_top_nav' ,link1:  (link_to 'Edit履歷表 ', edit_curriculum_vitae_info_users_path) ,link2: (link_to 'Create履歷表 ', new_basic_information_path) , link3: (link_to '首頁', root_path) %>
  <%= render 'shared/bootstrap/header' %>
    <!-- Portfolio Grid Section -->
    <section id="show_portfolio">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                 <div class="show_page">
                  <% if user_basic? %>
                   <%= link_to "#{session[:name]} - 顯示履歷表", show_curriculum_vitae_info_users_path, class: 'show_page_btn btn btn-success' %>
                  <% else %>
                  <h2><%= session[:name] %></h2>
                  <% end %>
                 </div>
                 </div>
            </div>
        </div>
    </section>